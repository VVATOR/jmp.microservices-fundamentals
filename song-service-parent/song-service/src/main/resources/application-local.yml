server:
  port: 8182

spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/song-service-db
    username: song
    password: s3cr3t

management:
  endpoints:
    web:
      exposure:
        include: "*"